# Reports

`latest.md` is generated from completed local runs with:

```powershell
python image-gen-lab/lab.py report
```

Generated images remain in the ignored `results/` folder. The report links to
those local files without copying multi-megabyte image assets into Git.

