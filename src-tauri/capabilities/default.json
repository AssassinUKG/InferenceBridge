{
  "identifier": "default",
  "description": "Capability for the main window — allows all app IPC commands and core events",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
